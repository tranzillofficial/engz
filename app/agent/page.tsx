import{getCurrentUser}from'@/lib/services/auth';import{getAgentByUserId,getAgentRegionStats,getAgentPayoutSummary}from'@/lib/services/agent';import{redirect}from'next/navigation';import Link from'next/link';import{AppShell,PageHeader,Card,StatCard}from'@/components';export const dynamic='force-dynamic';export default async function AgentDashboardPage(){const user=await getCurrentUser();if(!user||user.role!=='agent')redirect('/login');const agent=await getAgentByUserId(user.id);if(!agent)return <div className="p-8 text-center"><h2>حساب الوكيل غير مرتبط بمنطقة</h2></div>;const stats=await getAgentRegionStats(agent.region_id),payout=await getAgentPayoutSummary(agent.id),balance=Number((agent as any).commission_balance||0);const nav=[{href:'/agent',label:'الرئيسية',labelEn:'Overview',icon:<span>▦</span>},{href:'/agent/orders',label:'الطلبات',labelEn:'Orders',icon:<span>▣</span>},{href:'/agent/drivers',label:'الطيارين',labelEn:'Drivers',icon:<span>◉</span>},{href:'/agent/payments',label:'مستحقاتي',labelEn:'Payouts',badge:payout.pendingCount||undefined,icon:<span>ج</span>},{href:'/agent/complaints',label:'الشكاوى',labelEn:'Complaints',icon:<span>!</span>},{href:'/agent/profile',label:'حسابي',labelEn:'Account',icon:<span>⌂</span>}];return <AppShell header={<PageHeader title={`وكالة منطقة ${agent.region.name_ar||agent.region.name}`} titleEn="Regional Agent Hub" subtitle={`مرحباً ${agent.user.full_name}`}/>} navItems={nav}><div className="max-w-3xl mx-auto py-3 space-y-4"><div className="grid grid-cols-2 gap-3"><StatCard label="إجمالي الطيارين" labelEn="Drivers" value={stats.totalDrivers} color="primary"/><StatCard label="طيارين متصلين" labelEn="Online" value={stats.onlineDrivers} color="success"/><StatCard label="طلبات جارية" labelEn="Active" value={stats.activeOrders} color="warning"/><StatCard label="طلبات مكتملة" labelEn="Delivered" value={stats.deliveredOrders} color="info"/></div><div className="grid sm:grid-cols-2 gap-3"><Card className="p-4"><p className="text-xs font-bold text-slate-500">رصيد مستحق للسحب</p><p className="text-2xl font-black text-[#FA3802] mt-1">{balance.toLocaleString('ar-EG')} ج.م</p><Link href="/agent/payments" className="btn btn-primary btn-sm mt-3">طلب سحب</Link></Card><Card className="p-4"><p className="text-xs font-bold text-slate-500">طلبات سحب قيد المراجعة</p><p className="text-2xl font-black mt-1">{payout.pendingCount}</p><p className="text-[11px] text-slate-400 mt-1">التحويل يتم بواسطة الإدارة فقط</p></Card></div><Card className="p-4 space-y-2"><h3 className="text-sm font-black">إدارة المنطقة</h3><Link href="/agent/drivers" className="flex justify-between p-3 rounded-xl bg-slate-50 text-xs font-bold"><span>الطيارين</span><span>→</span></Link><Link href="/agent/drivers/new" className="flex justify-between p-3 rounded-xl bg-orange-50 text-xs font-bold text-[#FA3802]"><span>إضافة طيار تابع للوكالة</span><span>+</span></Link><Link href="/agent/orders" className="flex justify-between p-3 rounded-xl bg-slate-50 text-xs font-bold"><span>طلبات المنطقة</span><span>→</span></Link></Card></div></AppShell>}

import{getCurrentUser}from'@/lib/services/auth';import{getDriverByUserId}from'@/lib/services/drivers';import{getDriverWalletSummary}from'@/lib/services/commissions';import{createClient}from'@/lib/supabase/server';import{redirect}from'next/navigation';import{DriverWalletClient}from'@/components/driver/DriverWalletClient';export const dynamic='force-dynamic';export default async function Page(){const u=await getCurrentUser();if(!u||u.role!=='driver')redirect('/login');const d=await getDriverByUserId(u.id);if(!d)redirect('/login');const[w,db]=await Promise.all([getDriverWalletSummary(d.id),createClient()]);const{data:method}=await db.from('admin_payment_methods').select('method_key,method_name,account_name,account_number,instructions').eq('is_active',true).order('created_at',{ascending:false}).limit(1).maybeSingle();return <DriverWalletClient driver={d} currentBalance={w.currentBalance} totalCommissions={w.totalCommissions} totalPaid={w.totalPaid} transactions={w.transactions} paymentMethod={method as any}/>}
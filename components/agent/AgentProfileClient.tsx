'use client';
import {AppShell,PageHeader,Card,Button} from '@/components';
import {logoutAction} from '@/lib/actions/auth';
import type {AgentWithRegion} from '@/lib/services/agent';
export function AgentProfileClient({agent}:{agent:AgentWithRegion}){return <AppShell header={<PageHeader title="بيانات الوكيل" titleEn="Agent Profile" backHref="/agent"/>}><div className="max-w-md mx-auto py-3 space-y-4"><Card className="p-5 text-center"><div className="w-16 h-16 rounded-full bg-orange-50 text-[#FA3802] flex items-center justify-center mx-auto mb-3 text-2xl">🏢</div><h2 className="text-base font-black">{agent.user.full_name}</h2><p className="text-xs text-slate-500">{agent.user.email}</p><p className="text-xs text-slate-500 mt-1">منطقة: {agent.region.name_ar||agent.region.name}</p></Card><Card className="p-5 space-y-3"><h3 className="text-sm font-black">مستحقاتك</h3><p className="text-xs text-slate-500">طلبات السحب ورقم التحويل يتم إدخالها وقت طلب السحب فقط. التحويل نفسه يتم بواسطة الإدارة.</p><div className="p-3 rounded-xl bg-orange-50 text-xs font-bold text-[#FA3802]">رصيد مستحق: {Number((agent as any).commission_balance||0).toLocaleString('ar-EG')} ج.م</div></Card><form action={logoutAction}><Button type="submit" variant="outline" className="w-full text-red-600">تسجيل الخروج</Button></form></div></AppShell>}

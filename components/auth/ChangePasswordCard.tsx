'use client';import{useState}from'react';import{createClient}from'@/lib/supabase/client';import{Card,Button,Input,Alert}from'@/components';export default function ChangePasswordCard(){const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[state,setState]=useState<any>(null);const[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setState(null);if(password.length<6||password!==confirm){setState({type:'error',text:'كلمة المرور يجب أن تكون 6 أحرف على الأقل ومتطابقة'});return}setBusy(true);const{error}=await createClient().auth.updateUser({password});setBusy(false);setState(error?{type:'error',text:'فشل تغيير كلمة المرور'}:{type:'success',text:'تم تغيير كلمة المرور بنجاح'});if(!error){setPassword('');setConfirm('')}};return <Card className="p-4 space-y-3"><h3 className="text-sm font-black">تغيير كلمة المرور</h3>{state&&<Alert type={state.type}>{state.text}</Alert>}<form onSubmit={submit} className="space-y-2"><Input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة المرور الجديدة" required/><Input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="تأكيد كلمة المرور" required/><Button type="submit" disabled={busy} className="w-full">{busy?'جاري التغيير...':'تغيير كلمة المرور'}</Button></form></Card>}
